package org.study.testcode.repositroy;

import org.springframework.data.jpa.repository.JpaRepository;
import org.study.testcode.TestEntityGeneratedValue;

public interface TestEntityGeneratedValuedRepository extends JpaRepository<TestEntityGeneratedValue, Long> {
}
